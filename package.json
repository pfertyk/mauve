{
  "name": "mauve-markdown",
  "version": "0.1.1",
  "description": "Very simple Markdown viewer",
  "main": "./src/main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . --overwrite --all --icon=resources/logo.svg --prune=true --out=release --ignore=build",
    "deb64": "electron-installer-debian --arch=amd64 --config build/linux_x64.json",
    "rpm64": "electron-installer-redhat --arch=x86_64 --config build/linux_x64.json"
  },
  "keywords": [
    "markdown",
    "viewer"
  ],
  "author": "Paweł Fertyk <pfertyk@pfertyk.me>",
  "license": "MIT",
  "devDependencies": {
    "electron": "~1.6.2"
  },
  "dependencies": {
    "bluebird": "^3.5.0",
    "showdown": "^1.7.1"
  }
}
